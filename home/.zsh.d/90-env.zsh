export PATH="${HOME}/.local/bin:${PATH}"
export PATH="${HOME}/bin:${PATH}"
export PATH="${HOME}/bin.local:${PATH}"

test -f /etc/profile.d/google-cloud-cli.sh && source /etc/profile.d/google-cloud-cli.sh
