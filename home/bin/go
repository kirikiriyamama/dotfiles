#!/bin/bash
set -o errexit -o pipefail

bin=/usr/bin/go

if target="$(cat .go-version 2>/dev/null | sed -e 's/\([0-9]\+\.[0-9]\+\)\.0/\1/')"; then
  GOTOOLCHAIN="go${target}" ${bin} "${@}"
else
  ${bin} "${@}"
fi
